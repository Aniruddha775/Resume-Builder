import { describe, it } from 'vitest'

describe('ModernCleanTemplate', () => {
  it.todo('renders SAMPLE_RESUME to a non-empty Blob — skipped: react-pdf hangs in jsdom')
  it.todo('handles an empty resume without throwing — skipped: react-pdf hangs in jsdom')
})
