'use client'
import type { Editor } from '@tiptap/react'
interface ExperienceSectionProps { editor: Editor | null }
export function ExperienceSection({ editor: _editor }: ExperienceSectionProps) { return null }
