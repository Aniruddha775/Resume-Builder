---
phase: 01-foundation-layout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - tsconfig.json
  - next.config.ts
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/globals.css
  - src/lib/utils.ts
  - components.json
  - vitest.config.ts
  - src/__tests__/setup.ts
  - .prettierrc
  - eslint.config.mjs
autonomous: true
requirements:
  - FNDN-01
must_haves:
  truths:
    - "Running `pnpm dev` starts a Next.js 16 dev server without errors"
    - "Running `pnpm build` produces a successful production build"
    - "Running `pnpm vitest run` executes the test suite (even if 0 real tests, framework initializes)"
    - "Tailwind CSS v4 utility classes render correctly (no tailwind.config.js exists)"
    - "shadcn/ui is initialized and components can be added via CLI"
  artifacts:
    - path: "package.json"
      provides: "Project manifest with all Phase 1 dependencies"
      contains: "next"
    - path: "vitest.config.ts"
      provides: "Test framework configuration"
      contains: "vitest"
    - path: "src/app/globals.css"
      provides: "Tailwind v4 CSS-first config with theme tokens"
      contains: "@import"
    - path: "components.json"
      provides: "shadcn/ui configuration"
      contains: "shadcn"
  key_links:
    - from: "vitest.config.ts"
      to: "tsconfig.json"
      via: "path alias resolution"
      pattern: "tsconfigPaths"
    - from: "src/app/globals.css"
      to: "tailwindcss"
      via: "CSS import"
      pattern: "@import.*tailwindcss"
---

<objective>
Scaffold the Next.js 16 project with all Phase 1 dependencies, configure Tailwind CSS v4 with design tokens from UI-SPEC, initialize shadcn/ui, and set up the Vitest test framework.

Purpose: Establish the project foundation that all subsequent plans depend on. This is the irreversible decision point -- framework version, package manager, styling approach, and test infrastructure are set here.

Output: A buildable, testable Next.js 16 project with pnpm, Tailwind CSS v4, shadcn/ui, Zustand, Zod, and Vitest installed and configured.
</objective>

<execution_context>
@C:/Users/graem/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/graem/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-layout/01-CONTEXT.md
@.planning/phases/01-foundation-layout/01-RESEARCH.md
@.planning/phases/01-foundation-layout/01-UI-SPEC.md
@.planning/phases/01-foundation-layout/01-VALIDATION.md
@DESIGN.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 16 project and install all Phase 1 dependencies</name>
  <files>package.json, pnpm-lock.yaml, tsconfig.json, next.config.ts, src/app/layout.tsx, src/app/page.tsx, src/app/globals.css, eslint.config.mjs, .prettierrc</files>
  <read_first>
    - CLAUDE.md (tech stack versions, configuration notes for Next.js 16, Tailwind v4, pnpm)
    - .planning/phases/01-foundation-layout/01-RESEARCH.md (exact package versions, install commands, anti-patterns)
    - .planning/phases/01-foundation-layout/01-UI-SPEC.md (design tokens, font config, color values, Tailwind v4 theme config)
  </read_first>
  <action>
1. Install pnpm globally if not available: `npm install -g pnpm`

2. Scaffold Next.js 16 project in the current directory (Rese is already a git repo, so scaffold into a temp dir and move files):
   ```
   pnpm create next-app@latest rese-scaffold --typescript --tailwind --eslint --app --src-dir --use-pnpm --import-alias "@/*"
   ```
   Then move all generated files (except .git) from rese-scaffold/ into the project root. Remove the temp directory.

3. Install core Phase 1 dependencies:
   ```
   pnpm add zustand zod immer react-resizable-panels lucide-react
   ```

4. Install dev dependencies:
   ```
   pnpm add -D vitest @vitejs/plugin-react vite-tsconfig-paths jsdom @testing-library/react @testing-library/jest-dom prettier prettier-plugin-tailwindcss
   ```

5. Initialize shadcn/ui:
   ```
   pnpm dlx shadcn@latest init --defaults
   ```
   This creates components.json and sets up the ui/ directory with cn() utility in src/lib/utils.ts.

6. Add shadcn components for Phase 1:
   ```
   pnpm dlx shadcn@latest add resizable tabs separator tooltip
   ```
   After adding resizable, verify the generated file at src/components/ui/resizable.tsx compiles without TypeScript errors. If it uses old PanelGroup/PanelResizeHandle imports from react-resizable-panels v2/v3 API, update imports to match v4 API (per Pitfall 3 in RESEARCH.md).

7. Configure globals.css for Tailwind CSS v4 with design tokens from UI-SPEC:
   - The file MUST use `@import "tailwindcss";` (single import, NOT the v3 `@tailwind base; @tailwind components; @tailwind utilities;` directives)
   - Add `@theme` block with:
     ```css
     @theme {
       --font-sans: "Inter", ui-sans-serif, system-ui, sans-serif;
       --color-success: oklch(0.723 0.191 142.5);
       --color-warning: oklch(0.768 0.165 54.0);
       --color-danger: oklch(0.577 0.245 27.3);
     }
     ```
   - shadcn CSS variables (--background, --foreground, --primary, --muted, --border, etc.) are managed by shadcn's own `:root` block -- do NOT override them in @theme
   - Verify no tailwind.config.js or tailwind.config.ts exists in the project root. Delete if present.

8. Configure src/app/layout.tsx:
   - Import Inter font from `next/font/google` with `subsets: ['latin']`, `weight: ['400', '600']`
   - Apply font as CSS variable `--font-inter` on the `<html>` element
   - Set metadata: title "Rese", description "AI-powered resume builder with live ATS scoring"
   - Import globals.css

9. Set src/app/page.tsx to a minimal placeholder:
   ```tsx
   export default function Home() {
     return (
       <main className="h-dvh">
         <p>Rese</p>
       </main>
     )
   }
   ```
   This will be replaced by Plan 03 with the full layout.

10. Create .prettierrc with:
    ```json
    {
      "semi": false,
      "singleQuote": true,
      "tabWidth": 2,
      "trailingComma": "es5",
      "plugins": ["prettier-plugin-tailwindcss"]
    }
    ```

11. Add scripts to package.json:
    - `"test": "vitest run"`
    - `"test:watch": "vitest"`
    - `"test:coverage": "vitest run --coverage"`
    - `"format": "prettier --write ."`
    - `"format:check": "prettier --check ."`
    Ensure `"dev"`, `"build"`, `"start"` scripts exist from scaffold.

12. Review the generated AGENTS.md file if create-next-app produced one (per Pitfall 4 in RESEARCH.md). Keep it if it contains useful Next.js 16 patterns. CLAUDE.md takes precedence for project-level decisions.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains "next", "react", "zustand", "zod", "immer", "react-resizable-panels", "lucide-react" in dependencies
    - package.json contains "vitest", "@vitejs/plugin-react", "vite-tsconfig-paths", "jsdom", "prettier" in devDependencies
    - components.json exists in project root (shadcn initialized)
    - src/components/ui/resizable.tsx exists (shadcn resizable component installed)
    - src/components/ui/tabs.tsx exists (shadcn tabs component installed)
    - src/app/globals.css contains `@import "tailwindcss"` (NOT `@tailwind base`)
    - src/app/globals.css contains `@theme {` with `--font-sans`
    - No file named tailwind.config.js or tailwind.config.ts exists in project root
    - src/app/layout.tsx contains `Inter` font import from `next/font/google`
    - pnpm build exits with code 0
  </acceptance_criteria>
  <done>Next.js 16 project scaffolded, all dependencies installed, Tailwind v4 configured with CSS-first approach, shadcn/ui initialized with Phase 1 components, build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Configure Vitest test framework with path aliases and test setup</name>
  <files>vitest.config.ts, src/__tests__/setup.ts</files>
  <read_first>
    - tsconfig.json (path alias configuration to mirror in Vitest)
    - package.json (verify vitest is installed)
    - .planning/phases/01-foundation-layout/01-VALIDATION.md (Wave 0 requirements, test commands)
    - .planning/phases/01-foundation-layout/01-RESEARCH.md (dev dependency versions)
  </read_first>
  <action>
1. Create vitest.config.ts in project root:
   ```typescript
   import { defineConfig } from 'vitest/config'
   import react from '@vitejs/plugin-react'
   import tsconfigPaths from 'vite-tsconfig-paths'

   export default defineConfig({
     plugins: [react(), tsconfigPaths()],
     test: {
       environment: 'jsdom',
       globals: true,
       setupFiles: ['./src/__tests__/setup.ts'],
       include: ['src/**/*.{test,spec}.{ts,tsx}'],
       exclude: ['node_modules', '.next'],
     },
   })
   ```

2. Create src/__tests__/setup.ts:
   ```typescript
   import '@testing-library/jest-dom/vitest'

   // Mock localStorage for jsdom environment
   const localStorageMock = (() => {
     let store: Record<string, string> = {}
     return {
       getItem: (key: string) => store[key] ?? null,
       setItem: (key: string, value: string) => { store[key] = value },
       removeItem: (key: string) => { delete store[key] },
       clear: () => { store = {} },
       get length() { return Object.keys(store).length },
       key: (index: number) => Object.keys(store)[index] ?? null,
     }
   })()

   Object.defineProperty(globalThis, 'localStorage', {
     value: localStorageMock,
   })
   ```

3. Create a smoke test to verify the framework works at src/__tests__/smoke.test.ts:
   ```typescript
   import { describe, it, expect } from 'vitest'

   describe('Vitest setup', () => {
     it('runs tests', () => {
       expect(true).toBe(true)
     })

     it('has localStorage mock', () => {
       localStorage.setItem('test', 'value')
       expect(localStorage.getItem('test')).toBe('value')
       localStorage.clear()
     })
   })
   ```
  </action>
  <verify>
    <automated>pnpm vitest run --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - vitest.config.ts exists and contains `tsconfigPaths()` plugin
    - vitest.config.ts contains `environment: 'jsdom'`
    - vitest.config.ts contains `setupFiles: ['./src/__tests__/setup.ts']`
    - src/__tests__/setup.ts contains `@testing-library/jest-dom/vitest`
    - src/__tests__/setup.ts contains `localStorage` mock with getItem, setItem, removeItem, clear, length, key
    - src/__tests__/smoke.test.ts exists and contains `describe('Vitest setup'`
    - `pnpm vitest run` exits with code 0 and reports 2 passing tests
  </acceptance_criteria>
  <done>Vitest configured with jsdom environment, path aliases matching tsconfig, localStorage mock in setup file, and smoke test passing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> project | Third-party packages installed from npm |
| localStorage -> application | Data read from browser storage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | npm packages | mitigate | Use pnpm (content-addressable storage verifies integrity), pin exact versions in pnpm-lock.yaml, review AGENTS.md if generated |
| T-01-02 | Information Disclosure | .env files | mitigate | Verify .gitignore includes .env* patterns (Next.js scaffold includes this by default) |
| T-01-03 | Tampering | localStorage data | accept | No real data persisted in Phase 1 (placeholder content only). Zod validation added in Plan 02 for when data flows begin. |
</threat_model>

<verification>
1. `pnpm build` exits 0 (project compiles)
2. `pnpm vitest run` exits 0 (test framework works)
3. `pnpm dev` starts without errors on localhost:3000
4. No tailwind.config.js or tailwind.config.ts in project root
5. shadcn components installed: resizable, tabs, separator, tooltip
</verification>

<success_criteria>
A clean Next.js 16 project with all Phase 1 dependencies installed, Tailwind CSS v4 configured via CSS-first approach with design tokens, shadcn/ui initialized, and Vitest test framework operational. `pnpm build` and `pnpm vitest run` both pass.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-layout/01-01-SUMMARY.md`
</output>
